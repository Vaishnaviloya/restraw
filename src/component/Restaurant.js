import React from 'react'
import Menu from './Menu'
import MenuCard from './MenuCard'

const Restaurant = () => {
    return (
        <div>
            <MenuCard menu = {Menu}/>
        </div>
    )
}

export default Restaurant
